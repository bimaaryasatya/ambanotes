name: ambanotes
description: AI-powered document management and secretariat dashboard.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  lucide_icons: 0.257.0
  google_fonts: ^6.2.1
  flutter_staggered_animations: ^1.0.0
  image_picker: ^1.0.7
  file_picker: ^6.1.1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
